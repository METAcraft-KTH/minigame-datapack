# Fireball Bat
Functions `fireball:api/*` has everything you need.

You can configure stuff with functions `fireball:config/*`

An arena need to be created! Run: `/function fireball:setup/create_arena` for instructions.