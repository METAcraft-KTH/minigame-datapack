# ============================================================
# mingle:on/introstart
# Called by: main:superstate/1/macro_get_intro
# Executor:  Server
#
# Runs once when the intro starts.
# ============================================================

# Keep control points enabled for round 1.
tag @e[type=marker,tag=mingle.cp] remove mingle.cp.disabled
