These functions are called by `main:private/call` when the matching event happens, allowing you to do minigame-specific handling. Sorta like advancement trigger rewards!

Do not rename, add or remove any files in this and any subdirectories, unless you also add matching calls from `main`.