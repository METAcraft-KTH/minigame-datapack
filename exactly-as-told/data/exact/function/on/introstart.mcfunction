# ============================================================
# exact:on/introstart
# Called by: main:superstate/1/macro_get_intro
# Executor:  Server
# ============================================================

# Ensure the active arena marker tag exists before game start.
tag @e[type=marker,tag=exact.tp.1] add exact.tp.arena
